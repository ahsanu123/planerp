import { InputNoBorderDirective } from './input-no-border.directive';

describe('InputNoBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new InputNoBorderDirective();
    expect(directive).toBeTruthy();
  });
});
